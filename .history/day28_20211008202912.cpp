#include<bits/stdc++.h>

using namespace std;

int main(){
    int n,j;
    cout<<"Enter the size of the array : ";
    cin>>n;
    vector<int> arr;
    for(int i=0;i<n;++i) v.push_back(rand()%n);
    cout<<endl;
    for(int i=0;i<n;++i)cout<<arr[i]<<" ";
}